import React from 'react';

var Header = React.createClass({
  render() {
    var { headerText, size,hNumber, ...other } = this.props;
    return (
        <div>

        </div>
    );
  }
});

export default Header;
